#include <stdio.h>

int isPrime(int x, int knownPrimes[], int number0fKnownPrimes);

int main(void) {
	//定义数组
	const int number = 100;
	int prime[number];
	//初始化数组
	prime[0] = 2;
	int i;
	for (i = 1;i < number;i++)
	{
		prime[i] = 0;
	}
	//判断是否为素数并导入数组
	int count = 1;//count为数组有效部分的长度
	i = 3;
	while (count < number)
	{
		if (isPrime(i, prime, count))
		{
			prime[count++] = i;
		}
		/*调试
		{
			printf("i=%d \tcnt=%d\t", i, count);
			int i;
			for (i = 0;i < number;i++)
			{
				printf("%d\t", prime[i]);
			}
			printf("\n");
		}*/
		i++;
	}
	//输出素数
	for (i = 0;i < number;i++)
	{
		printf("%d", prime[i]);
		if ((i + 1) % 5!=0)printf("\t");
		else printf("\n");
	}
	return 0;
}

int isPrime(int x, int knownPrimes[], int number0fKnownPrimes) {
	int ret = 1;
	int i;
	for (i = 0;i < number0fKnownPrimes;i++)
	{
		if (x % knownPrimes[i] == 0)
		{
			ret = 0;
			break;
		}
	}
	return ret;
}